# 本机验证约定

用户正在同时使用这台电脑，已经要求测试不要反复弹窗打断操作。

- 常规 Electron 回归使用 `npm.cmd run test:e2e`，默认由 `tests/e2e/runtime.ts` 启动后台离屏实例。先构建，不能让旧构建忽略后台参数后弹出窗口。
- 直接编写启动脚本、打包冒烟或录屏时，同样必须处理窗口隐藏；采用独立 `ZHUMO_USER_DATA` 并设置 `ZHUMO_TEST_BACKGROUND=1`。使用旧版二进制前先检查是否支持该模式。
- 不要自行切回可见测试。仅在用户已经安排可见测试时段时使用 `ZHUMO_E2E_VISIBLE=1`，并缩小到需要真实桌面的场景。
- 后台测试使用隔离的剪贴板替身，不读写用户的系统剪贴板。真实系统剪贴板检查属于可见桌面测试。
- 后台离屏帧间隔不能标为正常屏幕帧率。记录执行模式，保留两种模式各自的报告与指标。
- 所有 Electron 窗口测试和录制依次运行。

完整入口和验证口径见 [后台测试说明](docs/e2e-testing.md)。
