# 清理时的目录归属核对

安装 41 修复交叉审查对安装 40 提出的三项问题。清理的文件名单仍只来自原程序清单；用户文稿、设置和完整资料恢复点保留。

- 首次准备与缓存重试共用同一来源推导。版本、profile、source、安装请求、运行时 ID、清单摘要及实际 source 目录身份均需对应。
- 候选认领、执行前以及 native 删除时检查完整目录链。native 从盘根逐级打开目录，拒绝 junction/reparse、实际路径偏离，并保持不共享写入/删除的目录句柄到文件删除结束；原 source 与父目录的身份也在持有的句柄上核对。
- 取得清理准入后重新读取文稿库绑定；保护目录集发生变化时，在删除前停止并释放准入。绑定记录不存在和绑定目标失联分别处理。

2026-09-09 源码验证：构建与类型检查通过；13 项清理/原生文件检查通过（`work/setup41-cleanup-units.log`，695.75 秒）。新增覆盖首次/缓存嵌套 junction、取得准入前改库、原目录替换且文件 inode 保留，以及 native 目录链检查。修改后的原生文件操作另有 4 项先行检查通过。交叉审查已确认原三项代码问题关闭。

安装 41 的完整 NSIS 流程触及 20 分钟测试上限，不能记作全流程通过。追踪显示逐文件启动 PowerShell 做完整目录检查带来过多开销；其独立清理进程随后完成 476 项程序副本清理，但超时后的后续重装步骤不在通过范围内。

安装 42 将候选认领前的目录链检查交给同一个 native `DirectoryChain`，保留全部祖先、实际路径和 reparse 校验。最终删除仍持有完整目录链及原 source/parent 身份到删除结束，减少重复进程启动。5 项原生检查与 4 项清理回归通过（330.77 秒），性能替换的交叉审查确认目录边界不回退。42 的完整包流程仍触及 20 分钟上限，不能记作通过。

## 安装 57：减少版本元数据的检查开销

`current-version.ts` 的路径属性核对也改用原生只读操作。调用前原有的目录链 `lstat`、文件类型、链接数及实际路径检查继续执行；原生层以只读属性权限打开对象，检查重解析点和最终路径。它不删除文件，不替代最终删除时持续持有的 `DirectoryChain`。

帮助程序从执行代码附近定位，兼容打包后的共享 chunk 与 `app.asar.unpacked`，不搜索用户文稿目录。缺少帮助程序，或旧帮助程序明确返回不支持的操作时，继续执行原有 PowerShell 验证；其它失败一律中止。使用提交 `30aaa6f` 的真实旧 C# 程序验证了兼容分支。

2026-09-09：相同 18 项版本检查由 103.49 秒降到 28.26 秒；9 项清理检查通过，用时 234.86 秒；4 项路径检查覆盖长中文路径、工作目录变化、链接及旧帮助程序兼容。日志分别为 `work/path57-before.log`、`work/path57-after.log`、`work/setup57-cleanup-units.log`、`work/path57-legacy-check.log`。完整 NSIS 流程的独立结果另记，不以这些专项检查替代。

完整 NSIS 57 流程随后通过，用时 17.7 分钟，其中安装包构建约 398 秒、首次安装 36.5 秒、同版重装 38.3 秒、两次恢复卸载分别约 238 / 225 秒。覆盖安装并发拒绝、阅读器运行中拒绝、过期卸载入口拒绝、真正帮助程序退出后的回滚、已提交移除的重试，以及卸载后重装。原目录 81 个文件、初始 65 个 / 后续 72 个 profile 文件按字节保留；密文、导入字体、主题与 HDR 设置、未命名草稿、用户后续改稿及运行目录中新加的 MD / 图片均通过检查。移出 160 个受管版本文件，清理 482 个程序副本。

证据为 `work/setup57-full.log` 与 `work/setup57/managed-installer-evidence.json`。对应 AI 预览包版本 `2.0.0-preview.ai.57`，程序清单 SHA-256 为 `cef310e2b8a50e42e3f95ffc0576926427af1e95c88eccabad85e94a286ff32d`。真实旧帮助程序兼容字段补丁晚于该包构建，另由上述专项验证覆盖；最终发行包仍须包含补丁并进行正式产品身份验证。

正式配置身份与发布准备继续进行，见[产品与资料身份](product-profile-identity.md)。
