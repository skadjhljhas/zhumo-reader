# 候选65：恢复原琉璃与单列工具栏

用户明确否定 rc.4 的两列左栏及新背景光场。最新修正是：背景恢复加入 HDR 前的最后版本，连光束也不要柔化，只增加 HDR 显示。不能再把“进一步优化”理解为改变该光场的构图、色调或纹理。

## 已实现

- `light.glsl` 与首次加入 HDR 之前的提交 `8743eb8` 完全一致，`lucent-art.css` 也恢复为同一版本；其对应最后桌面候选为31。原折光、四道光束、蓝紫色空气、流动和行为参数继续保留。
- `light.wgsl` 对照同一光场移植；仅在原构图计算后，为入射光增加扩展亮度。暗部和原蓝紫底色不整体提白。SDR/HDR 沿用现有设置入口。
- 左栏统一64像素、单列图标。移除两列网格、挤压的文件名、双按钮模式卡片。打开、目录、读写切换、注释、检索、标注、回声直接可用；新建、PDF、长卷、脉络和模型等收在“更多阅读工具”。文稿名及未保存提示仍有可访问入口。
- 光华使用真实文字的字形蒙版，金色明暗及亮斑在笔画内部缓缓移动，向字缘散光；取消 rc.4 覆盖普通正文的反光层及旧光华向上投射的大块光束。它与模型的字色和荧光仍是独立标注信息；没有光华的标注继续使用模型选择的字色和荧光。
- 光华小画布附着原段落的局部坐标，原生随滚动移动，原始文本及选择范围不改写。图形计算为GPU；没有可用GPU时保留SDR金字。导入字体沿用原字形定位与字体加载修复。

## 验证与边界

候选31、rc.4、候选65均实际以隔离资料在隐藏 Electron 中打开对照，图像在 `work/optics65/`。没有调用真实模型：标注用 localhost 固定流式样例走完整请求、解析与渲染链路。

类型检查与构建通过。候选65的实际打包程序通过两项流程：所有主题的单列操作、PDF入口与读写切换；字形蒙版的中文两端对齐、裁剪、GPOS。日志：`work/optics65-packaged-checks.log`。

实际打包的SDR、HDR光华均有非空字形，原文与光层滚动50像素的偏差为0。HDR纹理为 `rgba16float`，采样最大通道1.11328125，间隔700毫秒的平均通道变化0.00391958；证明有扩展亮度和动态。隐藏测试模拟HDR能力信号，不能作为物理显示器亮度或前台帧率的证据。日志 `work/optics65-packaged-visual.log`，数据 `work/optics65/packaged-hdr-evidence.json`。

程序：`../朱墨-AI细读/桌面候选65/win-unpacked/ZhuMo-AI.exe`，版本 `2.0.0-preview.ai.65`，80个程序文件，清单SHA-256：`c9e612f284c3fb76d87189cecb4c999109679291bd89ca32c2d1f992486ba84a`。未改用户密钥、字体设置和文稿库，未覆盖rc.4或候选10。此候选等待用户审美复核，不能沿用rc.4旧验收宣称新视觉已被接受。
