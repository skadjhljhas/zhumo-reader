# 白纸主题与左侧工作区

候选 59 新增「白纸」：纯白页面、黑色正文、灰阶图表与辅助界面。阅读时不创建装饰书首，也不显示「全书完」。Markdown 自己的 H1 和后续标题按正常正文排版显示；按当前主题导出 PDF 时也不添加装饰书首。

所有主题共用左侧文稿操作。原顶部整栏已移入左侧，包括文稿名、格式与未保存状态、阅读/编辑、新建、打开、PDF、目录、旁注和主题选择。阅读进度放在左侧文稿操作区底部。工作区采用两列紧凑按钮，窗口较矮时左侧独立滚动；文本标注菜单移至页面顶层，避免被滚动容器裁切。目录浮层的位置使用同一左栏宽度。

2026-09-09 检查：类型与修改范围 lint 通过；实际候选 59 核对全部 10 个主题的左侧操作可达性、白纸底色/字色、原 H1、装饰移除、阅读编辑切换和标注菜单内操作。另覆盖隐藏窗口、新建/恢复、EPUB、PDF、公式、默认文稿位置及选区交互。18 条流程中，16 条首轮通过，2 条因为测试仍寻找已合并的旧「打开文稿」入口而失败；改为当前入口后两条重验通过。

日志：`work/paper59-build.log`、`work/paper59-e2e.log`、`work/paper59-selection-replay.log`。截图：`work/paper59/paper-left.png`。候选程序清单 SHA-256 为 `46f94ca04b6611055e63da2f664ab41a9b9f2f18773765cc17a64bc8892b98d3`。

跨设备 ZIP 只包含清单中的 80 个程序文件、程序清单与使用说明，不复制本机密钥、文稿和资料目录；压缩包逐文件按清单校验，再校验全部 ZIP 条目。它是完整解压运行的 Windows 快照，独立于正式产品安装器验收。
