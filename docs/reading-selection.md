# 选中文字后的注释、解释与复制

选区动作由全局「阅读设置 → 选中文字后」决定。写用户注释、自动解释、自动复制是三个独立开关；默认全部关闭，单纯选字不弹窗。

启用写用户注释时，释放后即可输入，Enter保存、Shift+Enter换行，输入法确认不触发保存。用户注释自动带「用户注释」身份，并保存所选原文范围。解释依照系统提示词自动生成，两者同时开启时共用简洁浮层。复制无弹窗。

完整操作、精确范围的Markdown扩展、200k上下文、缓存与网页暂存边界见 [选区动作说明](selection-actions.md)。旧版「开启编辑，写注释／复制选段／问AI」操作菜单已被替换。