# 安装入口：覆盖、可恢复移除与重装已验证，尚不用于正式发布

40从卸载器独立环境接通程序副本清理，真实卸载/中断/重装流程通过，资料恢复点保持完整。具体清理边界与残留策略见[程序副本清理](program-cleanup.md)。旧版配置身份和最终发布仍待完成。

39补齐提交决定和终态重试等恢复边界，加入按同一文件实体操作的原生助手及发布前介质检查。最终包的安装/卸载和连续恢复失败流程串行通过；具体范围见[恢复边界](setup-recovery-boundaries.md)。bootstrap/恢复副本最终清理、旧版配置身份和最终发布仍在进行。

`build/managed-installer.nsi` 和 `src/main/setup-action.ts` 已接通实际 NSIS 编译与执行。安装验证37基于候选35，覆盖旧目录和整份profile，验证字体、HDR、加密配置、未命名草稿、运行中拒绝、同版重装、后续改稿保留、登记恢复与快捷方式归属。受管程序可恢复移除、实际帮助进程中断后的原生锁接管和重装也通过。参见[实际安装验证](managed-installer-validation.md)。

默认构建不生成 `out/main/setup-action.js`。只有显式设置 `ZHUMO_EXPERIMENTAL_SETUP=1` 才编入该原型入口；常规候选仍使用现有已验证的运行入口，不把此草稿作为安装器发布。

安装入口使用现有 `installVersion` 事务、独立版本目录与持久启动器。预检、UTF-16LE计划、回执读回、系统登记正常失败回滚和安装器互斥已落实。38使用v2版本日志接通[安装侧联合恢复](setup-install-recovery.md)，真实帮助进程退出与重试通过。独立bootstrap/恢复副本最终清理，以及原版与正式/预览资料并存的选择/迁移仍需完成。

现有 `docs/version-update.md` 和 `docs/release-readiness.md` 记录的是已通过的本地更新事务与尚未完成的发布要求。不能把草稿存在或静态检查通过当成真正安装升级已完成。
